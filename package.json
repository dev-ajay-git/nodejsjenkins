{
  "name": "dev-ajay-git",
  "version": "1.0.0",
  "description": "A Node.js project with Jenkins pipeline for testing, building, and deployment.",
  "scripts": {
    "test": "mocha __tests__/**/*.js",
    "build": "echo 'No build step defined'",
    "start": "node server.js",
    "sonar": "sonar-scanner",
    "deploy-nexus": "bash deploy-nexus.sh",
    "jacoco": "nyc mocha __tests__/**/*.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mocha": "^8.0.0",
    "nyc": "^15.1.0"
  },
  "devDependencies": {
    "webpack": "^5.0.0",
    "webpack-cli": "^4.0.0"
  }
}
